// GENERATED by scripts/migrate-markdown.ts — do not edit manually
import type { Operator } from '../types'

export const combinationOperators: Operator[] = [
	{
		name:        "merge",
		family:      'Combination' as const,
		signature:   '',
		description: "Flattens multiple Observables into one by blending their emissions",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["basic","combination"],
	},
	{
		name:        "concat",
		family:      'Combination' as const,
		signature:   '',
		description: "Emits values from each Observable sequentially, one after another",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["basic","combination"],
	},
	{
		name:        "race",
		family:      'Combination' as const,
		signature:   '',
		description: "Emits values from the first Observable to emit, unsubscribing from others",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["basic","combination"],
	},
	{
		name:        "forkJoin",
		family:      'Combination' as const,
		signature:   '',
		description: "Waits for all Observables to complete, then emits final values as an array",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["basic","combination"],
	},
	{
		name:        "combineLatest",
		family:      'Combination' as const,
		signature:   '',
		description: "Combines latest values from all Observables whenever any emits",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["value","coordination"],
	},
	{
		name:        "combineLatestAll",
		family:      'Combination' as const,
		signature:   '',
		description: "Flattens higher-order Observable using combineLatest strategy",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["value","coordination"],
	},
	{
		name:        "withLatestFrom",
		family:      'Combination' as const,
		signature:   '',
		description: "Combines source with latest values from other Observables",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["value","coordination"],
	},
	{
		name:        "zip",
		family:      'Combination' as const,
		signature:   '',
		description: "Combines values from Observables in strict sequential pairs",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["value","coordination"],
	},
	{
		name:        "mergeAll",
		family:      'Combination' as const,
		signature:   '',
		description: "Flattens higher-order Observable by merging inner Observables",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["higherorder","flattening"],
	},
	{
		name:        "concatAll",
		family:      'Combination' as const,
		signature:   '',
		description: "Flattens higher-order Observable by concatenating inner Observables",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["higherorder","flattening"],
	},
	{
		name:        "switchAll",
		family:      'Combination' as const,
		signature:   '',
		description: "Flattens higher-order Observable by switching to latest inner Observable",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["higherorder","flattening"],
	},
	{
		name:        "exhaust",
		family:      'Combination' as const,
		signature:   '',
		description: "Flattens higher-order Observable by ignoring new inner Observables while one is active",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["higherorder","flattening"],
	},
	{
		name:        "startWith",
		family:      'Combination' as const,
		signature:   '',
		description: "Prepends specified values before the source begins emitting",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["joining","patterns"],
	},
	{
		name:        "endWith",
		family:      'Combination' as const,
		signature:   '',
		description: "Appends specified values after the source completes",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["joining","patterns"],
	},
	{
		name:        "pairwise",
		family:      'Combination' as const,
		signature:   '',
		description: "Groups consecutive emissions into pairs [previous, current]",
		marble:      '',
		examples:    [
		{ title: "Merging Multiple Streams", code: "import { merge, interval } from 'rxjs';\nimport { map } from 'rxjs/operators';\n\nconst first$ = interval(1000).pipe(map(x => `First: ${x}`));\nconst second$ = interval(1500).pipe(map(x => `Second: ${x}`));\n\nmerge(first$, second$).subscribe(console.log);\n// Output: First: 0, Second: 0, First: 1, First: 2, Second: 1..." },
		{ title: "Sequential Concatenation", code: "import { concat, of } from 'rxjs';\nimport { delay } from 'rxjs/operators';\n\nconst first$ = of(1, 2, 3);\nconst second$ = of(4, 5, 6).pipe(delay(1000));\nconst third$ = of(7, 8, 9);\n\nconcat(first$, second$, third$).subscribe(console.log);\n// Output: 1, 2, 3, (wait 1s), 4, 5, 6, 7, 8, 9" }
		],
		seeAlso:     [],
		tags:        ["joining","patterns"],
	}
]
