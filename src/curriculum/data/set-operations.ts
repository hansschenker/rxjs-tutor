// GENERATED by scripts/migrate-markdown.ts — do not edit manually
import type { Topic } from '../types'

export const setOperationsOperators: Topic[] = [
	{
		name:        "distinct",
		category:      'SetOperations' as const,
		definition:   '',
		description: "Emits only values that have never been emitted before (entire history)",
		visual:      '',
		examples:    [
		{ title: "Removing All Duplicates", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["distinctness","operations"],
	},
	{
		name:        "distinctUntilChanged",
		category:      'SetOperations' as const,
		definition:   '',
		description: "Emits only when the current value differs from the previous",
		visual:      '',
		examples:    [
		{ title: "Removing All Duplicates", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["distinctness","operations"],
	},
	{
		name:        "distinctUntilKeyChanged",
		category:      'SetOperations' as const,
		definition:   '',
		description: "Emits only when a specified object key's value changes",
		visual:      '',
		examples:    [
		{ title: "Removing All Duplicates", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["distinctness","operations"],
	},
	{
		name:        "sequenceEqual",
		category:      'SetOperations' as const,
		definition:   '',
		description: "Compares two Observables to determine if they emit the same sequence",
		visual:      '',
		examples:    [
		{ title: "Removing All Duplicates", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", content: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["comparison","operations"],
	}
]
