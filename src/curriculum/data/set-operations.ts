// GENERATED by scripts/migrate-markdown.ts — do not edit manually
import type { Operator } from '../types'

export const setOperationsOperators: Operator[] = [
	{
		name:        "distinct",
		family:      'SetOperations' as const,
		signature:   '',
		description: "Emits only values that have never been emitted before (entire history)",
		marble:      '',
		examples:    [
		{ title: "Removing All Duplicates", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["distinctness","operations"],
	},
	{
		name:        "distinctUntilChanged",
		family:      'SetOperations' as const,
		signature:   '',
		description: "Emits only when the current value differs from the previous",
		marble:      '',
		examples:    [
		{ title: "Removing All Duplicates", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["distinctness","operations"],
	},
	{
		name:        "distinctUntilKeyChanged",
		family:      'SetOperations' as const,
		signature:   '',
		description: "Emits only when a specified object key's value changes",
		marble:      '',
		examples:    [
		{ title: "Removing All Duplicates", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["distinctness","operations"],
	},
	{
		name:        "sequenceEqual",
		family:      'SetOperations' as const,
		signature:   '',
		description: "Compares two Observables to determine if they emit the same sequence",
		marble:      '',
		examples:    [
		{ title: "Removing All Duplicates", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\nof(1, 2, 2, 3, 1, 4, 3, 5, 1).pipe(\n  distinct()\n).subscribe(console.log);\n// Output: 1, 2, 3, 4, 5" },
		{ title: "Distinct with Key Selector", code: "import { of } from 'rxjs';\nimport { distinct } from 'rxjs/operators';\n\ninterface User {\n  id: number;\n  name: string;\n}\n\nconst users$ = of<User>(\n  { id: 1, name: 'Alice' },\n  { id: 2, name: 'Bob' },\n  { id: 1, name: 'Alice Updated' },\n  { id: 3, name: 'Charlie' }\n);\n\nusers$.pipe(\n  distinct(user => user.id)\n).subscribe(console.log);\n// Output: \n// { id: 1, name: 'Alice' }\n// { id: 2, name: 'Bob' }\n// { id: 3, name: 'Charlie' }" }
		],
		seeAlso:     [],
		tags:        ["comparison","operations"],
	}
]
