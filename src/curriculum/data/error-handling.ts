// GENERATED by scripts/migrate-markdown.ts — do not edit manually
import type { Operator } from '../types'

export const errorHandlingOperators: Operator[] = [
	{
		name:        "catchError",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Catches errors and returns a new Observable or throws a new error",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["error","recovery"],
	},
	{
		name:        "onErrorResumeNext",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Continues with next Observable when an error occurs, ignoring the error",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["error","recovery"],
	},
	{
		name:        "retry",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Resubscribes to source Observable on error, with optional count",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["retry","strategies"],
	},
	{
		name:        "retryWhen",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Resubscribes based on custom logic using a notifier Observable",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["retry","strategies"],
	},
	{
		name:        "finalize",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Executes a callback when Observable completes or errors (cleanup)",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["error","finalization"],
	},
	{
		name:        "tap",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Performs side effects for errors without affecting the stream",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["error","finalization"],
	},
	{
		name:        "throwError",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Creates an Observable that immediately emits an error",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["error","creation"],
	},
	{
		name:        "throwIfEmpty",
		family:      'ErrorHandling' as const,
		signature:   '',
		description: "Emits error if source completes without emitting any values",
		marble:      '',
		examples:    [
		{ title: "Basic Error Catching", code: "import { of, throwError } from 'rxjs';\nimport { catchError, map } from 'rxjs/operators';\n\nconst source$ = throwError(() => new Error('Something went wrong!'));\n\nsource$.pipe(\n  catchError(error => {\n    console.error('Caught error:', error.message);\n    return of('Fallback value');\n  })\n).subscribe(\n  value => console.log('Value:', value),\n  error => console.error('Error:', error)\n);\n// Output: Caught error: Something went wrong!\n//         Value: Fallback value" },
		{ title: "HTTP Request with Fallback", code: "import { ajax } from 'rxjs/ajax';\nimport { catchError, of } from 'rxjs';\n\nconst userData$ = ajax.getJSON('/api/user').pipe(\n  catchError(error => {\n    console.error('API call failed:', error);\n    // Return cached or default data\n    return of({ name: 'Guest', id: null });\n  })\n);\n\nuserData$.subscribe(user => console.log('User:', user));" }
		],
		seeAlso:     [],
		tags:        ["error","creation"],
	}
]
