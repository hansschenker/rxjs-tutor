// GENERATED by scripts/migrate-markdown.ts — do not edit manually
import type { Topic } from '../types'

export const joinCreationOperators: Topic[] = [
	{
		name:        "buffer",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Collects source values into arrays, emitted when notifier Observable emits",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["buffering","joins"],
	},
	{
		name:        "bufferToggle",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Buffers using opening Observable and closing Observable factory",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["buffering","joins"],
	},
	{
		name:        "bufferWhen",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Buffers using closing selector function",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["buffering","joins"],
	},
	{
		name:        "bufferCount",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Buffers a specified number of values",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["buffering","joins"],
	},
	{
		name:        "bufferTime",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Buffers values for a specified time duration",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["buffering","joins"],
	},
	{
		name:        "window",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Groups source values into nested Observables based on notifier",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["windowing","joins"],
	},
	{
		name:        "windowToggle",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Windows using opening Observable and closing Observable factory",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["windowing","joins"],
	},
	{
		name:        "windowWhen",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Windows using closing selector function",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["windowing","joins"],
	},
	{
		name:        "windowCount",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Groups values into nested Observables of specified count",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["windowing","joins"],
	},
	{
		name:        "windowTime",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Groups values into nested Observables over time duration",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["windowing","joins"],
	},
	{
		name:        "combineLatestWith",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Combines with other Observables, emitting arrays on any source emission",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["complex","joins"],
	},
	{
		name:        "zipWith",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Combines with other Observables in strict sequential pairs",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["complex","joins"],
	},
	{
		name:        "mergeWith",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Merges with other Observables concurrently",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["complex","joins"],
	},
	{
		name:        "concatWith",
		category:    'JoinCreation' as const,
		definition:  '',
		description: "Concatenates with other Observables sequentially",
		visual:      '',
		examples:    [
		{ title: "Buffer with Notifier", content: "import { interval, fromEvent } from 'rxjs';\nimport { buffer } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst clicks$ = fromEvent(document, 'click');\n\nsource$.pipe(\n  buffer(clicks$)  // Buffer values until click\n).subscribe(buffered => {\n  console.log('Buffered values:', buffered);\n});\n// Output: Buffered values: [0, 1, 2, 3, 4] (on click)\n//         Buffered values: [5, 6, 7] (on next click)" },
		{ title: "BufferToggle for Dynamic Buffering", content: "import { interval, fromEvent } from 'rxjs';\nimport { bufferToggle, map } from 'rxjs/operators';\n\nconst source$ = interval(100);\nconst startBuffer$ = fromEvent(document, 'mousedown');\nconst stopBuffer$ = fromEvent(document, 'mouseup');\n\nsource$.pipe(\n  bufferToggle(\n    startBuffer$,\n    () => stopBuffer$  // Factory function for closing Observable\n  )\n).subscribe(buffered => {\n  console.log('Captured during mouse press:', buffered);\n});\n// Buffers values only while mouse is pressed" }
		],
		seeAlso:     [],
		tags:        ["complex","joins"],
	}
]
