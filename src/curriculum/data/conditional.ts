// GENERATED by scripts/migrate-markdown.ts — do not edit manually
import type { Topic } from '../types'

export const conditionalOperators: Topic[] = [
	{
		name:        "every",
		category:      'Conditional' as const,
		definition:   '',
		description: "Tests if all emitted values satisfy a predicate",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["value","testing"],
	},
	{
		name:        "find",
		category:      'Conditional' as const,
		definition:   '',
		description: "Emits the first value that satisfies a predicate, then completes",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["value","testing"],
	},
	{
		name:        "findIndex",
		category:      'Conditional' as const,
		definition:   '',
		description: "Emits the index of the first value satisfying a predicate",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["value","testing"],
	},
	{
		name:        "isEmpty",
		category:      'Conditional' as const,
		definition:   '',
		description: "Emits true if source completes without emitting, false otherwise",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["value","testing"],
	},
	{
		name:        "defaultIfEmpty",
		category:      'Conditional' as const,
		definition:   '',
		description: "Emits a default value if the source completes without emitting",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["stream","selection"],
	},
	{
		name:        "iif",
		category:      'Conditional' as const,
		definition:   '',
		description: "Subscribes to one of two Observables based on a condition evaluated at subscription time",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["stream","selection"],
	},
	{
		name:        "throwIfEmpty",
		category:      'Conditional' as const,
		definition:   '',
		description: "Throws an error if the source completes without emitting any values",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["stream","selection"],
	},
	{
		name:        "sequenceEqual",
		category:      'Conditional' as const,
		definition:   '',
		description: "Compares two Observables to see if they emit the same sequence",
		visual:      '',
		examples:    [
		{ title: "Testing All Values with Every", content: "import { of } from 'rxjs';\nimport { every } from 'rxjs/operators';\n\nof(2, 4, 6, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? true\n\nof(2, 4, 5, 8, 10).pipe(\n  every(x => x % 2 === 0)\n).subscribe(result => console.log('All even?', result));\n// Output: All even? false" },
		{ title: "Finding First Match", content: "import { of } from 'rxjs';\nimport { find, findIndex } from 'rxjs/operators';\n\nconst numbers$ = of(1, 2, 3, 4, 5);\n\n// Find first value > 3\nnumbers$.pipe(\n  find(x => x > 3)\n).subscribe(value => console.log('Found:', value));\n// Output: Found: 4\n\n// Find index of first value > 3\nnumbers$.pipe(\n  findIndex(x => x > 3)\n).subscribe(index => console.log('Found at index:', index));\n// Output: Found at index: 3" }
		],
		seeAlso:     [],
		tags:        ["comparative","operations"],
	}
]
