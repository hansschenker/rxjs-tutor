// GENERATED by scripts/migrate-markdown.ts — do not edit manually
import type { Operator } from '../types'

export const concurrencyOperators: Operator[] = [
	{
		name:        "observeOn",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Controls the scheduler for downstream operators and subscription",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["scheduler","control"],
	},
	{
		name:        "subscribeOn",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Controls the scheduler for the Observable's subscription and unsubscription",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["scheduler","control"],
	},
	{
		name:        "mergeMap",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Flattens with configurable concurrency limit",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["parallel","execution"],
	},
	{
		name:        "concatMap",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Sequential execution (concurrency of 1)",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["parallel","execution"],
	},
	{
		name:        "switchMap",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Cancellation-based concurrency (only latest inner subscription)",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["parallel","execution"],
	},
	{
		name:        "exhaustMap",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Ignores new while busy (prevents concurrent inner subscriptions)",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["parallel","execution"],
	},
	{
		name:        "mergeAll",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Flattens with max concurrent subscriptions",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["concurrency","limiting"],
	},
	{
		name:        "concatAll",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Subscribes to inner Observables sequentially",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["concurrency","limiting"],
	},
	{
		name:        "switchAll",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Switches to latest, canceling previous",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["concurrency","limiting"],
	},
	{
		name:        "exhaust",
		family:      'Concurrency' as const,
		signature:   '',
		description: "Ignores new inner Observables while one is active",
		marble:      '',
		examples:    [
		{ title: "Controlling Subscription Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { subscribeOn, tap } from 'rxjs/operators';\n\nconsole.log('Before subscription');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Value:', x)),\n  subscribeOn(asyncScheduler) // Defer subscription to async context\n).subscribe();\n\nconsole.log('After subscription call');\n\n// Output:\n// Before subscription\n// After subscription call\n// Value: 1\n// Value: 2\n// Value: 3" },
		{ title: "Controlling Notification Context", code: "import { of, asyncScheduler } from 'rxjs';\nimport { observeOn, tap } from 'rxjs/operators';\n\nconsole.log('Start');\n\nof(1, 2, 3).pipe(\n  tap(x => console.log('Before observeOn:', x)),\n  observeOn(asyncScheduler), // Move emissions to async context\n  tap(x => console.log('After observeOn:', x))\n).subscribe();\n\nconsole.log('End');\n\n// Output:\n// Start\n// Before observeOn: 1\n// Before observeOn: 2\n// Before observeOn: 3\n// End\n// After observeOn: 1\n// After observeOn: 2\n// After observeOn: 3" }
		],
		seeAlso:     [],
		tags:        ["concurrency","limiting"],
	}
]
